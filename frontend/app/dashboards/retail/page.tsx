import { RetailDashboard } from "@/components/dashboard/RetailDashboard";

export const metadata = {
  title: "Retail Dashboard | QDT Nexus",
  description: "Mobile-first beginner-friendly trading signals in plain English. Simple interface with 'Looks Good', 'Heads Up', and 'Wait & See' signals.",
};

export default function RetailPage() {
  return <RetailDashboard />;
}
